"use client";

export function WorkflowBuilder() {
  return (
    <div className="h-full flex items-center justify-center text-gray-500">
      Workflow builder — drag nodes to build automations
    </div>
  );
}

export default WorkflowBuilder;
