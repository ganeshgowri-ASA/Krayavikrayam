# RAG → TBE deliverables pipeline (Python)
# Deterministic; pin major versions to keep CI reproducible.

# Pinecone Assistant client (high-level wrapper around the Assistant API)
pinecone>=5.0,<7.0
pinecone-plugin-assistant>=1.5,<2.0

# Spreadsheet writer (preserves VBA, links, formulas, merged cells, images)
openpyxl>=3.1,<4.0
Pillow>=10.0,<13.0  # required by openpyxl to read/write embedded images

# Word document writer for vendor clarification matrix
python-docx>=1.1,<2.0

# Email composition (uses stdlib email.message; no extra deps needed)

# Networking / retries
requests>=2.31,<3.0

# Tests
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
