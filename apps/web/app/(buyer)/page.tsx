import { PlaceholderPage } from "@/components/placeholder-page";

export default function HomePage() {
  return (
    <PlaceholderPage
      title="Home"
      description="Action-required cards and recent notifications land here in a follow-up session (B2)."
    />
  );
}
